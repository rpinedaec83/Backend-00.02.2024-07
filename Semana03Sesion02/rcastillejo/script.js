///tomamos los primeros 8 digitos del dni
let strDNI = prompt("Escribe tu DNI");
let strNumeroVeriricador = prompt("Escribe tu numero verificador")

try {
    //Multiplica cada uno de los ocho dígitos por una secuencia de números del 2 al 9, 
    //en orden descendente.


    alert("Tu DNI es Valido")
} catch (error) {
    //capturamos el error
}

